'use client'

/**
 * 예약 폼 하단용 자리. 브라우저 전용 스크래치가 붙으면 여기서 표시.
 * (현재는 UI 없음 — import 깨짐 방지)
 */
export default function ReservationLocalScratchList({ reservationId }: { reservationId?: string }) {
  void reservationId
  return null
}
