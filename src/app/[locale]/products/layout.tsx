import type { ReactNode } from 'react'

/** Segment layout only — page-level SEO lives on listing page / [id] layout. */
export default function ProductsSegmentLayout({ children }: { children: ReactNode }) {
  return children
}
